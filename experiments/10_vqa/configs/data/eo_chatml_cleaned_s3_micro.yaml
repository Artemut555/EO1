# Minimal sample for ~10s packing smoke test (S3 images). first:10, use with local_2gpu_pack_micro.
mm_datasets:
  - json_path: /home/jovyan/zavarza/projects/extra/eo/data/chatml_full_eo_cleaned/interleave-free_chat.jsonl
    format: chatml
    vision_backend: s3
    s3_bucket: gigaeye-data
    sampling_strategy: first:10
